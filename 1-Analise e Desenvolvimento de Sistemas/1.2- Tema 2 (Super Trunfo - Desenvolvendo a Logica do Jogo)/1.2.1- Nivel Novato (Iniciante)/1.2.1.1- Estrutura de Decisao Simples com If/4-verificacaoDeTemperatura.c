#include <stdio.h>

int main(){
    // Declaração de variaveis
    float temperatura;
    // Comando de atribuição
    temperatura = 32.0;
    // Execução da Estrutura condicional If
    if (temperatura >= 30.0){
        printf("A previsão do Tempo informa: %.1f graus de temperatura no Rio de Janeiro.\n", temperatura);
        printf("A temperatura está alta, que calor!\n");
    }

}